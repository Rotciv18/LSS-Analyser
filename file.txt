program Test3; {programa para teste do lexico}
var
   NUMERO  : real;
   final1 : real;
procedure fatorial (n : integer) : integer;
var
    final : real;
    x : real;
    fatorial2 : real;
begin
    if n <= 1 then
        x := n * fatorial
    else
        x := 1;
    x := x * 3
end;
procedure fatorial2 (n : integer) : integer;
var
    final : real;
    x : real;
    fatorial1 : real;
begin
    if n <= 1 then
        x := n * fatorial
    else
        x := 1;
    x := x * 3
end;
begin  {tente gerar um erro usando um caracter não permitido.. tipo $}
   NUMERO := 3.5*5+7-9 + fatorial(3);
   if (NUMERO >= 20) and (NUMERO <=90) and (final1 > NUMERO) then
      NUMERO := 10 / 3;
   NUMERO := NUMERO + 1
end.